# Frontend environment variables (Vite)
# Copy to .env.local — this file is safe to commit.

# Base URL of the running backend API.
# In Docker mode this is handled by vite proxy; leave blank for proxy mode.
# Set explicitly if running frontend standalone against a remote server.
VITE_API_BASE_URL=http://localhost:8000
